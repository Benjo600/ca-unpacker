# Work done on Friday, 4 September 2026, 8:22 PM–9:17 PM IST (UTC+5:30)

Session log for CA Unpacker. Continued the same evening’s parser/compare work. Offline local parse only (no cloud OCR).

## Shipped

**bugs-to-fix.md item 1 — bank statement → Excel parsing — locked in.**

Wrapped narration and page-break rows no longer drop transactions, so later dates and amounts stay aligned with the statement. Benny confirmed: “ok this works lock this in.”

## Goal

Fix item 1 on `bugs-to-fix.md`: extract bank statement rows to Excel from scanned image PDFs, locally.

## What was built and verified

- Kotak image PDF parsed with RapidOCR plus Docling table structure.
- Side-by-side compare UI at `http://127.0.0.1:8765/index.html` (statement page images on the left, extracted table on the right).
- First extract: 67 rows, unverified. Running-balance break around row 22; dates and amounts looked shifted versus the PDF.
- Root cause: wrapped narration / page-break rows were dropped. The PDF skips transaction 22 across pages and leaves a leftover “Payment from” wrap; later rows then looked date-jumped.
- Fix:
  1. A date-less table row with its own debit/credit and balance is treated as a new transaction and inherits the previous date.
  2. Small running-balance gaps are repaired when the printed balance implies a missing wrap row.
- Coverage added in `test_stage3_bank.py`.

## Git

- **`c517216` on `main`, pushed:** “Fix bank statement wraps so later dates and amounts stay aligned.” Touched parser, tables, Docling, OCR, requirements, and tests.
- Left uncommitted: `apps/web/.netlify/` and `scripts/compare_kotak_parse.py` (hardcoded local PDF path).
- **`bugs-to-fix.md` item 1** struck through as done (notes the wrap/page-break and running-balance repair at `c517216`).
- Branch **`cursor/mark-bank-parse-todo-done`**, commit **`fbbafbf`:** “Mark bank statement-to-Excel parsing as done on the bugs list.” Pushed to origin. Not merged to `main` as of this write-up (`main` remains at `c517216`).

## Still open

Items 2–8 on `bugs-to-fix.md` remain open (Restart behaviour, persist firm/folder/client names, scanned/layout PDF processing, UI modernization, per-client profiles, citations, in-app file preview).
